using SparkFlow.Server.Application.Services; namespace SparkFlow.Server.Application.Features.Sessions.StartSession; public sealed class StartSessionHandler { private readonly SessionService _service; public StartSessionHandler(SessionService service){_service=service;} public Task<bool> Handle(StartSessionCommand command, CancellationToken cancellationToken=default)=>_service.StartAsync(command.SessionId,cancellationToken);}
